# Role B runtime dependencies
# schemas.py uses Pydantic v2 APIs: ConfigDict, model_validate, and model_rebuild.
pydantic>=2.9,<3
httpx>=0.27,<1
pytest>=8,<9
aiosqlite>=0.20,<1
fastapi>=0.115,<1
uvicorn>=0.30,<1
python-dotenv>=1.0,<2

# Optional Phase 3 provider dependency. Install with: pip install -r requirements-openai.txt
openai>=1.0,<3

# Optional MCP transport; not required for the Role B API.
mcp>=1.0,<2
