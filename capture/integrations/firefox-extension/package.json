{
  "name": "intent-firefox-companion",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && zip -qr dist/intent-firefox.xpi manifest.json background.js content.js"
  }
}
